# ==============================================================================
# Building AI Agents for SRE & AIOps · Dependencies
# ==============================================================================
# The agent engine uses Python's standard library (urllib, json, subprocess).
# Only PyYAML is required for loading prompts.yaml.

pyyaml>=6.0.1

